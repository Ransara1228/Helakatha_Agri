spring:
  datasource:
    # H2 Database (for development - in-memory)
    url: jdbc:h2:mem:helaketha_agri;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver
    
    # SQL Server (for production - uncomment when SQL Server is running)
    # url: jdbc:sqlserver://localhost:1433;databaseName=helaketha_agri;encrypt=false;trustServerCertificate=true
    # username: sa
    # password: ransara@1234
    # driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      
  h2:
    console:
      enabled: true
      path: /h2-console
